
guard 'haml', :input => 'haml', :output => 'include' do
  watch(/^.+\.haml$/)
end

guard 'livereload' do
 watch(/^.+\.html$/)
 watch(%r{include/.+\.html})
 watch(%r{css/.+\.css})
 watch(%r{javascript/.+\.js})
end

