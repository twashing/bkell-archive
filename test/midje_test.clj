(ns midje_test
  (:use clojure.test)
  (:use midje.sweet)
)


;; we can get a default page "/"



;; we can login with Gmail
;;   GITkit calls should be a separate workflow - call from repl
;;   result will be a running session



;; we can login with Yahoo
;;   GITkit calls should be a separate workflow - call from repl
;;   result will be a running session



;; user can't CRUD unless logged in



;; user can only CRUD on his entities



