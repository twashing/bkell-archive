(ns add-test
  
  (:use [clojure.test]
        [monger.operators]
  )
  (:require 
            [bkell.commands.add :as addk]
            [bkell.domain :as domain]
            [monger.core :as mg]
            [monger.collection :as mc]
  )

)

