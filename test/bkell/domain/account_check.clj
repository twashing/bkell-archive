(ns bkell.domain.account-check
  (:require [bkell.domain.account :as da]
            [clojure.test.check :as tc]
            [clojure.test.check.generators :as gen]
            [clojure.test.check.properties :as prop]))
